# crouter — configurazione di R1 per la fase 3 (solo forwarding statico)
interface eth1 10.0.1.1/24
interface eth2 10.0.12.1/30
interface eth3 10.0.14.1/30
# static <rete>/<prefisso> via <gateway>
static 10.0.4.0/24 via 10.0.12.2
static 10.0.23.0/30 via 10.0.12.2
static 10.0.34.0/30 via 10.0.14.2
