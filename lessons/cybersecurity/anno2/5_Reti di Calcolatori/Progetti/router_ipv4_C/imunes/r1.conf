# crouter — configurazione per il nodo R1 in IMUNES
# (nomi interfaccia eth0/eth1/eth2 come li assegna IMUNES; gli IP vivono
#  in crouter, non a livello kernel del nodo)
interface eth0 10.0.1.1/24 rip
interface eth1 10.0.12.1/30 rip
interface eth2 10.0.14.1/30 rip
